// @generated by openapi-codegen from specs/petstore.yaml
// Do not edit by hand.

pub mod create_pet {
    use ::serde::{Deserialize, Serialize};
    use ::struct_patch::Patch;
    use super::shared::*;

    /// Error types.
    pub mod error {
        /// Error from a `TryFrom` or `FromStr` implementation.
        pub struct ConversionError(::std::borrow::Cow<'static, str>);

        impl ::std::error::Error for ConversionError {}

        impl ::std::fmt::Display for ConversionError {
            fn fmt(
                &self,
                f: &mut ::std::fmt::Formatter<'_>,
            ) -> Result<(), ::std::fmt::Error> {
                ::std::fmt::Display::fmt(&self.0, f)
            }
        }

        impl ::std::fmt::Debug for ConversionError {
            fn fmt(
                &self,
                f: &mut ::std::fmt::Formatter<'_>,
            ) -> Result<(), ::std::fmt::Error> {
                ::std::fmt::Debug::fmt(&self.0, f)
            }
        }

        impl From<&'static str> for ConversionError {
            fn from(value: &'static str) -> Self {
                Self(value.into())
            }
        }

        impl From<String> for ConversionError {
            fn from(value: String) -> Self {
                Self(value.into())
            }
        }
    }

    /// `CreatePetRequest`
    #[serde_with::skip_serializing_none]
    #[cfg_attr(feature = "schemars", derive(schemars::JsonSchema))]
    #[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Patch)]
    #[serde(rename_all = "camelCase")]
    #[patch(attribute(serde_with::skip_serializing_none))]
    #[patch(attribute(derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)))]
    #[patch(attribute(serde(default, rename_all = "camelCase")))]
    #[cfg_attr(feature = "schemars", patch(attribute(derive(schemars::JsonSchema))))]
    pub struct CreatePetRequest {
        #[patch(name = "Option<CategoryPatch>")]
        pub category: ::std::option::Option<Category>,
        pub idempotency_key: ::std::option::Option<::std::string::String>,
        pub name: ::std::string::String,
        pub photo_urls: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
        pub received_from: ::std::option::Option<::std::string::String>,
        pub wants_newsletter: ::std::option::Option<bool>,
    }
}

pub mod get_pet {
    use ::serde::{Deserialize, Serialize};
    use ::struct_patch::Patch;
    use super::shared::*;

    /// Error types.
    pub mod error {
        /// Error from a `TryFrom` or `FromStr` implementation.
        pub struct ConversionError(::std::borrow::Cow<'static, str>);

        impl ::std::error::Error for ConversionError {}

        impl ::std::fmt::Display for ConversionError {
            fn fmt(
                &self,
                f: &mut ::std::fmt::Formatter<'_>,
            ) -> Result<(), ::std::fmt::Error> {
                ::std::fmt::Display::fmt(&self.0, f)
            }
        }

        impl ::std::fmt::Debug for ConversionError {
            fn fmt(
                &self,
                f: &mut ::std::fmt::Formatter<'_>,
            ) -> Result<(), ::std::fmt::Error> {
                ::std::fmt::Debug::fmt(&self.0, f)
            }
        }

        impl From<&'static str> for ConversionError {
            fn from(value: &'static str) -> Self {
                Self(value.into())
            }
        }

        impl From<String> for ConversionError {
            fn from(value: String) -> Self {
                Self(value.into())
            }
        }
    }
}

pub mod shared {
    use ::serde::{Deserialize, Serialize};
    use ::struct_patch::Patch;
    use super::create_pet::*;
    use super::get_pet::*;

    /// Error types.
    pub mod error {
        /// Error from a `TryFrom` or `FromStr` implementation.
        pub struct ConversionError(::std::borrow::Cow<'static, str>);

        impl ::std::error::Error for ConversionError {}

        impl ::std::fmt::Display for ConversionError {
            fn fmt(
                &self,
                f: &mut ::std::fmt::Formatter<'_>,
            ) -> Result<(), ::std::fmt::Error> {
                ::std::fmt::Display::fmt(&self.0, f)
            }
        }

        impl ::std::fmt::Debug for ConversionError {
            fn fmt(
                &self,
                f: &mut ::std::fmt::Formatter<'_>,
            ) -> Result<(), ::std::fmt::Error> {
                ::std::fmt::Debug::fmt(&self.0, f)
            }
        }

        impl From<&'static str> for ConversionError {
            fn from(value: &'static str) -> Self {
                Self(value.into())
            }
        }

        impl From<String> for ConversionError {
            fn from(value: String) -> Self {
                Self(value.into())
            }
        }
    }

    /// `Category`
    #[serde_with::skip_serializing_none]
    #[cfg_attr(feature = "schemars", derive(schemars::JsonSchema))]
    #[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Patch)]
    #[serde(rename_all = "camelCase")]
    #[patch(attribute(serde_with::skip_serializing_none))]
    #[patch(attribute(derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)))]
    #[patch(attribute(serde(default, rename_all = "camelCase")))]
    #[cfg_attr(feature = "schemars", patch(attribute(derive(schemars::JsonSchema))))]
    pub struct Category {
        pub name: ::std::option::Option<::std::string::String>,
        #[patch(name = "Option<CategoryRefPatch>")]
        pub parent: ::std::option::Option<CategoryRef>,
    }

    /// `CategoryRef`
    #[serde_with::skip_serializing_none]
    #[cfg_attr(feature = "schemars", derive(schemars::JsonSchema))]
    #[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Patch)]
    #[serde(rename_all = "camelCase")]
    #[patch(attribute(serde_with::skip_serializing_none))]
    #[patch(attribute(derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)))]
    #[patch(attribute(serde(default, rename_all = "camelCase")))]
    #[cfg_attr(feature = "schemars", patch(attribute(derive(schemars::JsonSchema))))]
    pub struct CategoryRef {
        pub slug: ::std::option::Option<::std::string::String>,
    }

    /// `Dog`
    #[serde_with::skip_serializing_none]
    #[cfg_attr(feature = "schemars", derive(schemars::JsonSchema))]
    #[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Patch)]
    #[serde(rename_all = "camelCase")]
    #[patch(attribute(serde_with::skip_serializing_none))]
    #[patch(attribute(derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)))]
    #[patch(attribute(serde(default, rename_all = "camelCase")))]
    #[cfg_attr(feature = "schemars", patch(attribute(derive(schemars::JsonSchema))))]
    pub struct Dog {
        #[serde(flatten)]
        pub pet: Pet,
        pub bark_loudness: ::std::option::Option<i32>,
    }

    /// `Pet`
    #[serde_with::skip_serializing_none]
    #[cfg_attr(feature = "schemars", derive(schemars::JsonSchema))]
    #[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Patch)]
    #[serde(rename_all = "camelCase")]
    #[patch(attribute(serde_with::skip_serializing_none))]
    #[patch(attribute(derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)))]
    #[patch(attribute(serde(default, rename_all = "camelCase")))]
    #[cfg_attr(feature = "schemars", patch(attribute(derive(schemars::JsonSchema))))]
    pub struct Pet {
        #[patch(name = "Option<CategoryPatch>")]
        pub category: ::std::option::Option<Category>,
        pub created_at: ::std::option::Option<::std::string::String>,
        pub id: ::std::string::String,
        pub name: ::std::string::String,
        pub status: ::std::option::Option<PetStatus>,
        pub tag_count: ::std::option::Option<i32>,
    }

    /// `PetStatus`
    #[cfg_attr(feature = "schemars", derive(schemars::JsonSchema))]
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub enum PetStatus {
        #[serde(rename = "available")]
        Available,
        #[serde(rename = "pending")]
        Pending,
        #[serde(rename = "sold")]
        Sold,
    }

    impl ::std::fmt::Display for PetStatus {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
            match *self {
                Self::Available => f.write_str("available"),
                Self::Pending => f.write_str("pending"),
                Self::Sold => f.write_str("sold"),
            }
        }
    }

    impl ::std::str::FromStr for PetStatus {
        type Err = self::error::ConversionError;
        fn from_str(
            value: &str,
        ) -> ::std::result::Result<Self, self::error::ConversionError> {
            match value {
                "available" => Ok(Self::Available),
                "pending" => Ok(Self::Pending),
                "sold" => Ok(Self::Sold),
                _ => Err("invalid value".into()),
            }
        }
    }

    impl ::std::convert::TryFrom<&str> for PetStatus {
        type Error = self::error::ConversionError;
        fn try_from(
            value: &str,
        ) -> ::std::result::Result<Self, self::error::ConversionError> {
            value.parse()
        }
    }

    impl ::std::convert::TryFrom<&::std::string::String> for PetStatus {
        type Error = self::error::ConversionError;
        fn try_from(
            value: &::std::string::String,
        ) -> ::std::result::Result<Self, self::error::ConversionError> {
            value.parse()
        }
    }

    impl ::std::convert::TryFrom<::std::string::String> for PetStatus {
        type Error = self::error::ConversionError;
        fn try_from(
            value: ::std::string::String,
        ) -> ::std::result::Result<Self, self::error::ConversionError> {
            value.parse()
        }
    }

    impl ::std::default::Default for PetStatus {
        fn default() -> Self {
            Self::Available
        }
    }
}

/// Generated API client: `client::Client`, its builder, the
/// `auth` providers, and the `support` error/helper surface.
pub mod client {
    pub mod auth {
        use super::support::{Error, OperationInfo};

        /// Authorizes outgoing requests.
        ///
        /// The client calls [`AuthProvider::authorize`] with every request builder before the body is
        /// attached; implementations add whatever credentials the API expects. Custom providers (e.g.
        /// from the `ext` module) can implement this trait to change auth behavior without touching
        /// generated code.
        #[::async_trait::async_trait]
        pub trait AuthProvider: ::std::marker::Send + ::std::marker::Sync {
            /// Attach credentials to `request`. `op` identifies the
            /// operation being sent, so providers can specialize.
            async fn authorize(
                &self,
                request: ::reqwest_middleware::RequestBuilder,
                op: &OperationInfo,
            ) -> ::std::result::Result<::reqwest_middleware::RequestBuilder, Error>;
        }

        /// No authentication: requests pass through untouched. The
        /// builder default.
        #[derive(Debug, Clone, Copy, Default)]
        pub struct NoAuth;

        #[::async_trait::async_trait]
        impl AuthProvider for NoAuth {
            async fn authorize(
                &self,
                request: ::reqwest_middleware::RequestBuilder,
                _op: &OperationInfo,
            ) -> ::std::result::Result<::reqwest_middleware::RequestBuilder, Error> {
                ::std::result::Result::Ok(request)
            }
        }

        /// A fixed token attached as `Authorization: Bearer <token>` on
        /// every request.
        #[derive(Clone)]
        pub struct StaticBearer {
            token: ::std::string::String,
        }

        impl StaticBearer {
            pub fn new(token: impl ::std::convert::Into<::std::string::String>) -> Self {
                Self { token: token.into() }
            }
        }

        #[::async_trait::async_trait]
        impl AuthProvider for StaticBearer {
            async fn authorize(
                &self,
                request: ::reqwest_middleware::RequestBuilder,
                _op: &OperationInfo,
            ) -> ::std::result::Result<::reqwest_middleware::RequestBuilder, Error> {
                ::std::result::Result::Ok(request.bearer_auth(&self.token))
            }
        }
    }

    pub mod support {
        /// Identifies one API operation to hooks and error rendering.
        #[derive(Debug, Clone, Copy, PartialEq, Eq)]
        pub struct OperationInfo {
            /// The spec's `operationId`, verbatim.
            pub operation_id: &'static str,
            /// The uppercase HTTP method.
            pub method: &'static str,
            /// The spec's path template, e.g. `/pets/{petId}`.
            pub path: &'static str,
        }

        /// A registered request-body hook: runs against every serialized JSON request body before it is
        /// sent. See `ClientBuilder::body_hook`.
        pub type BodyHook = dyn ::std::ops::Fn(
            &OperationInfo,
            &mut ::serde_json::Value,
        ) + ::std::marker::Send + ::std::marker::Sync;

        /// Errors surfaced by generated client methods.
        #[derive(Debug)]
        pub enum Error {
            /// Authorizing the request failed (e.g. an OAuth2 token
            /// fetch); the message carries the underlying detail.
            Auth { op: &'static str, message: ::std::string::String },
            /// Serializing the request body to JSON failed.
            Serialize { op: &'static str, source: ::serde_json::Error },
            /// Building or sending the request failed (connection,
            /// middleware, or reading the response body).
            Request { op: &'static str, source: ::reqwest_middleware::Error },
            /// The server answered outside the 2xx range; the raw
            /// response body is carried for diagnostics.
            Status {
                op: &'static str,
                status: ::reqwest::StatusCode,
                body: ::std::string::String,
            },
            /// A 2xx response body failed to decode: `source` carries
            /// serde's line/column diagnostics and `body` the raw text.
            Decode {
                op: &'static str,
                source: ::serde_json::Error,
                body: ::std::string::String,
            },
        }

        impl ::std::fmt::Display for Error {
            fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
                match self {
                    Error::Auth { op, message } => {
                        write!(f, "{op}: authorization failed: {message}")
                    }
                    Error::Serialize { op, source } => {
                        write!(f, "{op}: request body serialization failed: {source}")
                    }
                    Error::Request { op, source } => {
                        write!(f, "{op}: request failed: {source}")
                    }
                    Error::Status { op, status, body } => {
                        write!(f, "{op}: response failed ({status}): {body}")
                    }
                    Error::Decode { op, source, body } => {
                        write!(
                            f,
                            "{op}: response decode failed: {source} (line {line}, column {column}); raw body: {body}",
                            line = source.line(), column = source.column(),
                        )
                    }
                }
            }
        }

        impl ::std::error::Error for Error {
            fn source(
                &self,
            ) -> ::std::option::Option<&(dyn ::std::error::Error + 'static)> {
                match self {
                    Error::Serialize { source, .. } | Error::Decode { source, .. } => {
                        ::std::option::Option::Some(source)
                    }
                    Error::Request { source, .. } => ::std::option::Option::Some(source),
                    Error::Auth { .. } | Error::Status { .. } => {
                        ::std::option::Option::None
                    }
                }
            }
        }

        /// Percent-encode one path segment: RFC 3986 unreserved
        /// characters pass through, everything else is encoded byte by
        /// byte (UTF-8).
        pub fn encode_path(segment: &str) -> ::std::string::String {
            let mut encoded = ::std::string::String::with_capacity(segment.len());
            for byte in segment.bytes() {
                match byte {
                    b'A'..=b'Z'
                    | b'a'..=b'z'
                    | b'0'..=b'9'
                    | b'-'
                    | b'.'
                    | b'_'
                    | b'~' => {
                        encoded.push(byte as char);
                    }
                    other => {
                        encoded.push_str(&::std::format!("%{other:02X}"));
                    }
                }
            }
            encoded
        }

        /// Read a response as text and decode it as JSON.
        ///
        /// Non-2xx statuses become [`Error::Status`] carrying the raw body. The 2xx body is read as
        /// text first and decoded via `serde_json::from_str` (rather than `Response::json`) so decode
        /// failures surface serde's line/column diagnostics alongside the raw body ([`Error::Decode`]).
        pub async fn decode_json<T: ::serde::de::DeserializeOwned>(
            op: &'static str,
            response: ::reqwest::Response,
        ) -> ::std::result::Result<T, Error> {
            let status = response.status();
            let body = response
                .text()
                .await
                .map_err(|source| Error::Request {
                    op,
                    source: source.into(),
                })?;
            if !status.is_success() {
                return ::std::result::Result::Err(Error::Status { op, status, body });
            }
            ::serde_json::from_str(&body)
                .map_err(|source| Error::Decode { op, source, body })
        }

        /// Check a body-less success response: non-2xx statuses become
        /// [`Error::Status`] carrying the raw body.
        pub async fn expect_success(
            op: &'static str,
            response: ::reqwest::Response,
        ) -> ::std::result::Result<(), Error> {
            let status = response.status();
            if status.is_success() {
                return ::std::result::Result::Ok(());
            }
            let body = response.text().await.unwrap_or_default();
            ::std::result::Result::Err(Error::Status { op, status, body })
        }

        impl ::std::default::Default for Error {
            fn default() -> Self {
                Self::Auth {
                    op: ::std::default::Default::default(),
                    message: ::std::default::Default::default(),
                }
            }
        }
    }

    use self::auth::AuthProvider;
    use self::support::{BodyHook, Error, OperationInfo};

    /// Client for Petstore (v1.0.0).
    ///
    /// Construct via [`Client::builder`], which takes the base URL (the spec declares no
    /// `servers`).
    #[derive(Clone)]
    pub struct Client {
        base_url: ::std::string::String,
        client: ::reqwest_middleware::ClientWithMiddleware,
        auth: ::std::sync::Arc<dyn AuthProvider>,
        body_hooks: ::std::vec::Vec<::std::sync::Arc<BodyHook>>,
    }

    impl ::std::fmt::Debug for Client {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
            f.debug_struct("Client")
                .field("base_url", &self.base_url)
                .finish_non_exhaustive()
        }
    }

    /// Builder for [`Client`].
    pub struct ClientBuilder {
        base_url: ::std::string::String,
        client: ::std::option::Option<::reqwest_middleware::ClientWithMiddleware>,
        auth: ::std::sync::Arc<dyn AuthProvider>,
        body_hooks: ::std::vec::Vec<::std::sync::Arc<BodyHook>>,
    }

    impl ClientBuilder {
        /// Start a builder. The spec declares no `servers`, so
        /// the base URL is required here.
        pub fn new(base_url: impl ::std::convert::Into<::std::string::String>) -> Self {
            Self {
                base_url: base_url.into(),
                client: ::std::option::Option::None,
                auth: ::std::sync::Arc::new(auth::NoAuth),
                body_hooks: ::std::vec::Vec::new(),
            }
        }

        /// Override the base URL operation paths are appended
        /// to. A trailing `/` is trimmed at build time.
        pub fn base_url(
            mut self,
            base_url: impl ::std::convert::Into<::std::string::String>,
        ) -> Self {
            self.base_url = base_url.into();
            self
        }

        /// Supply the HTTP client requests are sent through: a
        /// `reqwest_middleware::ClientWithMiddleware` carrying a middleware stack, or a plain
        /// `reqwest::Client` (converted middleware-free via `From`). Defaults to a fresh plain client.
        pub fn client(
            mut self,
            client: impl ::std::convert::Into<::reqwest_middleware::ClientWithMiddleware>,
        ) -> Self {
            self.client = ::std::option::Option::Some(client.into());
            self
        }

        /// Set the authentication provider; see [`auth`] for the
        /// generated implementations. Defaults to
        /// [`auth::NoAuth`].
        pub fn auth(mut self, auth: impl AuthProvider + 'static) -> Self {
            self.auth = ::std::sync::Arc::new(auth);
            self
        }

        /// Register a request-body hook: after a request body is serialized to JSON and before it is
        /// sent, every hook runs against the `serde_json::Value` in registration order. The seam for
        /// cross-cutting request edits (inject a tenant field, fill a default) without forking
        /// generated code; `OperationInfo` tells the hook which operation is sending.
        pub fn body_hook(
            mut self,
            hook: impl ::std::ops::Fn(
                &OperationInfo,
                &mut ::serde_json::Value,
            ) + ::std::marker::Send + ::std::marker::Sync + 'static,
        ) -> Self {
            self.body_hooks.push(::std::sync::Arc::new(hook));
            self
        }

        pub fn build(self) -> Client {
            Client {
                base_url: self.base_url.trim_end_matches('/').to_string(),
                client: self.client.unwrap_or_else(|| ::reqwest::Client::new().into()),
                auth: self.auth,
                body_hooks: self.body_hooks,
            }
        }
    }

    impl Client {
        /// Start building a client; see [`ClientBuilder`].
        pub fn builder(
            base_url: impl ::std::convert::Into<::std::string::String>,
        ) -> ClientBuilder {
            ClientBuilder::new(base_url)
        }

        /// The configured base URL (trailing `/` trimmed).
        pub fn base_url(&self) -> &str {
            &self.base_url
        }

        /// `POST /pets`
        pub async fn create_pet(
            &self,
            body: &super::create_pet::CreatePetRequest,
        ) -> ::std::result::Result<super::shared::Pet, Error> {
            const OP: OperationInfo = OperationInfo {
                operation_id: "createPet",
                method: "POST",
                path: "/pets",
            };
            let url = ::std::format!("{}/pets", self.base_url);
            let mut request = self.client.request(::reqwest::Method::POST, url);
            request = self.auth.authorize(request, &OP).await?;
            let mut body = ::serde_json::to_value(body)
                .map_err(|source| Error::Serialize {
                    op: OP.operation_id,
                    source,
                })?;
            for hook in &self.body_hooks {
                hook(&OP, &mut body);
            }
            request = request.json(&body);
            let response = request
                .send()
                .await
                .map_err(|source| Error::Request {
                    op: OP.operation_id,
                    source,
                })?;
            support::decode_json(OP.operation_id, response).await
        }

        /// `GET /pets/{petId}`
        pub async fn get_pet(
            &self,
            pet_id: &str,
        ) -> ::std::result::Result<super::shared::Pet, Error> {
            const OP: OperationInfo = OperationInfo {
                operation_id: "getPet",
                method: "GET",
                path: "/pets/{petId}",
            };
            let pet_id = support::encode_path(pet_id);
            let url = ::std::format!("{}/pets/{}", self.base_url, pet_id);
            let mut request = self.client.request(::reqwest::Method::GET, url);
            request = self.auth.authorize(request, &OP).await?;
            let response = request
                .send()
                .await
                .map_err(|source| Error::Request {
                    op: OP.operation_id,
                    source,
                })?;
            support::decode_json(OP.operation_id, response).await
        }
    }
}
